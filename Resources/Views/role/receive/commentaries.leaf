#extend("role/toolkit")
#export("role") {– #if(langfra) {Recevoir des commentaires} ##else() {Receive Commentaries}}
#export("head") {
<title>#if(langfra) {Recevoir des commentaires} ##else() {Receive Commentaries}</title>
}
#export("body") {
#embed("receivebreadcrumbs")
#embed("commentarytable")
}
#export("scripts") {

}
